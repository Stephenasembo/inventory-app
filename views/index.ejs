<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Inventory</title>
  </head>
  <body>
    <h1>Welcome to my inventory</h1>
    <div>
      Choose a category in the inventory to view:
      <ul>
        <% categories.forEach((category) => { %>
          <li><a href="/categories/<%= category.id%>/page"><%= category.name%></a></li>
        <% }) %>
      </ul>
      Create a new category <a href="/categories/new">here</a>.
    </div>
  </body>
</html>